@extends('layouts.public')

@section('content')
    <section class="bg-slate-50 dark:bg-gray-900">
        <div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 py-14 space-y-10">
            <div class="text-center space-y-3">
                <span class="inline-flex items-center gap-2 rounded-full bg-white px-4 py-1 text-sm font-medium text-indigo-600 shadow-sm dark:bg-gray-800 dark:text-indigo-200">
                    <svg class="h-4 w-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8.25 21h7.5M6 21a3 3 0 01-3-3v-7.5A3 3 0 014.223 8.06l5.5-3.143a3 3 0 012.554 0l5.5 3.143A3 3 0 0121 10.5V18a3 3 0 01-3 3" />
                    </svg>
                    {{ __('Menu Coffee Shop') }}
                </span>
                <h1 class="text-4xl font-bold tracking-tight text-gray-900 dark:text-white">
                    {{ __('Temukan dan Pesan Minuman Favorit Anda') }}
                </h1>
                <p class="text-base text-gray-600 dark:text-gray-300">
                    {{ __('Lihat menu terbaru, scan QR di meja untuk memesan, dan nikmati layanan cepat dari barista kami.') }}
                </p>
            </div>

            <form method="GET" action="{{ route('home') }}" class="mx-auto max-w-3xl">
                <label for="search" class="sr-only">{{ __('Cari menu') }}</label>
                <div class="flex items-center gap-3 rounded-2xl border border-indigo-100 bg-white px-4 py-2 shadow-sm focus-within:border-indigo-300 dark:border-gray-700 dark:bg-gray-800">
                    <span class="text-gray-400 dark:text-gray-500">
                        <svg class="h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 21l-4.35-4.35m0 0A7.5 7.5 0 104.5 4.5a7.5 7.5 0 0012.15 12.15z" />
                        </svg>
                    </span>
                    <input
                        id="search"
                        name="q"
                        type="search"
                        value="{{ $search }}"
                        placeholder="{{ __('Cari kopi, minuman, atau makanan ringan...') }}"
                        class="flex-1 bg-transparent text-base text-gray-800 placeholder:text-gray-400 focus:outline-none dark:text-gray-100 dark:placeholder:text-gray-500"
                    >
                    <button type="submit"
                        class="inline-flex items-center gap-2 rounded-xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow hover:bg-indigo-500 focus:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500">
                        <svg class="h-4 w-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4.5 12H19.5m0 0l-5.25 5.25M19.5 12l-5.25-5.25" />
                        </svg>
                        {{ __('Cari') }}
                    </button>
                </div>
                @if ($search)
                    <p class="mt-3 text-center text-sm text-gray-500 dark:text-gray-400">
                        {{ __('Menampilkan hasil untuk pencarian ":term".', ['term' => $search]) }}
                    </p>
                @endif
            </form>

            <div class="grid gap-4 sm:flex sm:flex-wrap sm:justify-center sm:gap-6 text-sm text-gray-600 dark:text-gray-300">
                <span class="inline-flex items-center gap-2 rounded-full bg-white px-4 py-2 shadow-sm dark:bg-gray-800">
                    <span class="h-2 w-2 rounded-full bg-emerald-500"></span> {{ __('Biji kopi premium pilihan') }}
                </span>
                <span class="inline-flex items-center gap-2 rounded-full bg-white px-4 py-2 shadow-sm dark:bg-gray-800">
                    <span class="h-2 w-2 rounded-full bg-indigo-500"></span> {{ __('Pemesanan tanpa kontak lewat QR') }}
                </span>
                <span class="inline-flex items-center gap-2 rounded-full bg-white px-4 py-2 shadow-sm dark:bg-gray-800">
                    <span class="h-2 w-2 rounded-full bg-purple-500"></span> {{ __('Pembayaran aman Midtrans') }}
                </span>
            </div>

            <div class="grid gap-12">
                @forelse ($categories as $category)
                    <div class="space-y-6">
                        <div>
                            <h2 class="text-2xl font-semibold text-gray-900 dark:text-gray-100">
                                {{ $category->name }}
                            </h2>
                            <p class="text-sm text-gray-500 dark:text-gray-400">
                                {{ $category->description ?: __('Pilihan menu terbaik dari barista kami.') }}
                            </p>
                        </div>

                        <div class="grid gap-6 sm:grid-cols-2 xl:grid-cols-3">
                            @forelse ($category->menuItems as $item)
                                @php
                                    $isLowStock = $item->stock <= $item->low_stock_threshold;
                                @endphp
                                <article class="group flex h-full flex-col overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-lg dark:border-gray-700 dark:bg-gray-800">
                                    <div class="aspect-[3/4] w-full overflow-hidden bg-gray-100 dark:bg-gray-700">
                                        <img src="{{ $item->image_url ?? 'https://via.placeholder.com/400x520?text=Menu' }}" alt="{{ $item->name }}" class="h-full w-full object-cover object-center" loading="lazy">
                                    </div>
                                    <div class="flex flex-1 flex-col gap-4 p-4">
                                        <div class="space-y-1.5">
                                            <h3 class="text-base font-semibold text-gray-900 dark:text-gray-100">
                                                {{ $item->name }}
                                            </h3>
                                            <p class="text-sm text-gray-500 dark:text-gray-400">
                                                {{ $item->description ?: __('Belum ada deskripsi untuk menu ini.') }}
                                            </p>
                                        </div>
                                        <div class="mt-auto flex items-center justify-between text-sm">
                                            <div>
                                                <p class="font-semibold text-gray-900 dark:text-gray-100">
                                                    Rp{{ number_format($item->price, 0, ',', '.') }}
                                                </p>
                                                <p class="text-xs {{ $isLowStock ? 'text-amber-600' : 'text-emerald-600' }}">
                                                    {{ $isLowStock ? __('Sisa stok: :qty', ['qty' => $item->stock]) : __('Stok tersedia: :qty', ['qty' => $item->stock]) }}
                                                </p>
                                            </div>
                                            @auth
                                                @if (auth()->user()->isCustomer())
                                                    <form method="POST" action="{{ route('cart.store') }}">
                                                        @csrf
                                                        <input type="hidden" name="menu_item_id" value="{{ $item->id }}">
                                                        <input type="hidden" name="quantity" value="1">
                                                        <button type="submit" class="inline-flex items-center gap-2 rounded-full bg-indigo-500 px-4 py-2 text-xs font-semibold text-white shadow-sm hover:bg-indigo-400 focus:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500">
                                                            {{ __('Tambah') }}
                                                        </button>
                                                    </form>
                                                @endif
                                            @else
                                                <a href="{{ route('login') }}" class="inline-flex items-center gap-2 rounded-full bg-indigo-500 px-4 py-2 text-xs font-semibold text-white shadow-sm hover:bg-indigo-400 focus:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500">
                                                    {{ __('Login untuk pesan') }}
                                                </a>
                                            @endauth
                                        </div>
                                    </div>
                                </article>
                            @empty
                                <p class="text-sm text-gray-500 dark:text-gray-400">{{ __('Belum ada menu di kategori ini.') }}</p>
                            @endforelse
                        </div>
                    </div>
                @empty
                    <div class="rounded-3xl border border-dashed border-gray-300 bg-white px-6 py-12 text-center shadow-sm dark:border-gray-700 dark:bg-gray-800">
                        <h3 class="text-lg font-semibold text-gray-900 dark:text-gray-100">{{ __('Belum ada menu yang ditampilkan.') }}</h3>
                        <p class="mt-2 text-sm text-gray-500 dark:text-gray-400">
                            {{ __('Silakan kembali lagi nanti atau hubungi barista kami untuk bantuan.') }}
                        </p>
                    </div>
                @endforelse
            </div>
        </div>
    </section>
@endsection

